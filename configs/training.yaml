training:
  algorithm: ppo
  gamma: 0.99
  gae_lambda: 0.95
  learning_rate: 0.0003
  rollout_steps: 512
  minibatch_size: 64
  epochs: 4
  clip_epsilon: 0.20
  entropy_coef: 0.01
  value_coef: 0.5
  max_grad_norm: 0.5
